server {
    listen 80;
    server_name _;
    client_max_body_size 20M;

    # Gzip settings for performance optimization
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml;

    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        # Support React client-side routing (BrowserRouter)
        try_files $uri $uri/ /index.html;
    }

    # Proxy API requests to AWS Internal Load Balancer
    location /api/ {
        proxy_pass http://${BACKEND_ALB_DNS}/api/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Proxy Django admin panel
    location /admin/ {
        proxy_pass http://${BACKEND_ALB_DNS}/admin/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Proxy static Django files (Django serves these using WhiteNoise)
    location /static/ {
        proxy_pass http://${BACKEND_ALB_DNS}/static/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Proxy local uploaded media folder fallback (mostly uses Cloudinary)
    location /media/ {
        proxy_pass http://${BACKEND_ALB_DNS}/media/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
